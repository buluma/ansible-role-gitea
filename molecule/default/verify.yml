---
- name: Verify
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: Run gitea --version
      ansible.builtin.command:
        cmd: gitea --version
      changed_when: false

    - name: Check port 3000
      ansible.builtin.wait_for:
        port: 3000
